alias aliyunpan='/opt/aliyunpan/aliyunpan'
alias c='clear'
alias dps="docker ps"
alias dls="docker images"
alias drmc="docker container rm"
alias drmi="docker image rm"
alias e='exit'
alias emacs='emacs -nw'
alias l='ls'
alias la='ls -a'
alias ll='ls -lh'
alias lla='ls -alh'
alias sn='shutdown now'
alias vi='nvim'
alias vim='nvim'

if [ -e "/usr/local/bin/mv" ]; then
  alias mv='mv -ig'
fi

if [ -e "/usr/local/bin/cp" ]; then
  alias cp='cp -ig'
fi
