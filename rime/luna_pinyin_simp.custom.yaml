schema:
  schema_id: luna_pinyin_simp

patch:
  translator/dictionary: luna_pinyin.extended
  switches:
    - name: zh_simp
      reset: 1
      # states: ["漢", "汉"]
    - name: ascii_mode # 0-中 1-西
      reset: 1
      # states: ["中", "西"]
    - name: full_shape
      # states: [ 半, 全 ]
      reset: 0
    - name: ascii_punct
      # states: [ 。，, ．， ]
      reset: 1
